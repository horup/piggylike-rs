mod atlases;
mod tiles;
/*mod things;
use engine::*;

pub fn update(world) {
    for thing in world.things {
    }
}

pub fn main() {
    atlases::define();
    tiles::define();
    things::define();

    load_map("./assets/maps/test.tmj");
}*/

pub fn main(engine) {
    atlases::define(engine);
    tiles::define(engine);
}